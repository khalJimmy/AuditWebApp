# PostgreSQL Database Connection URL (Vercel Postgres, Neon, Supabase, Cloud SQL, Railway, etc.)
DATABASE_URL=postgres://user:password@localhost:5432/casagrand_audit
POSTGRES_URL=

# SMTP Email Relay Configuration
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=audit.operations@casagrand.co.in

# Server Port
PORT=3000
